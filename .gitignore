# Node
node_modules/

# Frontend build output (regenerated by `pnpm build` from the shared desktop src/)
dist/

# Rust build output (the aarch64 .so etc. — multi-GB)
src-tauri/target/

# macOS
.DS_Store

# NOTE: src-tauri/gen/android build outputs (.gradle, app/build, local.properties,
# keystores) are ignored by the tauri-generated .gitignore files inside gen/android.
